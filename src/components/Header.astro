---
import { Image } from "astro:assets"
import hero from "../../public/hero.webp"
---

<container class="text-white flex justify-center items-center flex-col">
  <div class="grid grid-cols-1 md:grid-cols-2 gap-16 mt-48">
    <div>
      <h1 class="mt-24 text-6xl mb-8">Welcome to our coffee shop!</h1>
      <p class="text-2xl mb-8">
        Indulge in the rich aroma and exquisite flavors of our carefully crafted
        coffee.
      </p>
      <div class="flex gap-8 flex-col md:flex-row">
        <button
          class="bg-red-400 hover:bg-red-600 px-8 py-2 rounded-3xl text-white"
          >Contact me</button
        >
        <button
          class="bg-red-400 hover:bg-red-600 px-8 py-2 rounded-3xl text-white"
          >More Products</button
        >
      </div>
    </div>
    <div>
      <Image src={hero} class="rounded-xl" loading="lazy" alt="Hero" />
    </div>
  </div>
</container>
